interface:
  display_name: "GitHub Submit Change"
  short_description: "Create consistent branches, commits, and pull requests"
  default_prompt: "Use $github-submit-change to create a branch, commit the intended changes, and open a pull request."
